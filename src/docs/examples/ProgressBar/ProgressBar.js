import React from "react";
import Progressbar from "../../../common/ProgressBar/Progressbar";

/** Progress bar */
export default function ExampleProgressBar() {
  return <Progressbar width={100} height={10} percentage={40} />;
}
